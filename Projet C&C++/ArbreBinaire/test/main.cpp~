#include <string> //Permet de créer des chaines de caracteres
#include <fstream> //Permet la manipulation des fichiers
#include <iostream>
#include<limits>
using namespace std;
int main(void){
ifstream in("bdd.txt"); //Ouverture en mode lecture de "bdd.txt"
string ligne; //Création d'une chaine de caractere
int nbLignes = 0;
//while(getline(in, ligne)) //On lit chaque ligne du fichier que l'on stoke dans "ligne"
//nbLignes++;      
      while(in.ignore(numeric_limits<int>::max(), '\n'))
	nbLignes++;

in.close(); //On ferme le fichier
 cout<<nbLignes<<endl;
return 0;
}
